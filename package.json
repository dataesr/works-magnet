{
  "name": "works-magnet",
  "version": "0.3.2",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "concurrently": "^8.0.1"
  },
  "scripts": {
    "build": "rm -rf server/dist && npm -w client run build -- --mode ${npm_config_mode} --emptyOutDir --outDir ../server/dist",
    "client": "npm -w client run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "deploy": "git switch main && git pull origin main --rebase --tags && git merge origin staging && npm version patch -ws && git add **/package.json && git add package-lock.json && npm version patch --include-workspace-root --force && git push origin main --tags && git switch staging",
    "preview": "npm run build --mode=${npm_config_mode} && npm -w server start",
    "server": "npm -w server run dev",
    "start": "npm run dev"
  }
}
